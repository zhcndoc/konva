title: HTML5 Canvas 所有 Tween 控制教程
---

要使用 Konva 播放、暂停、反向、重置、完成和查找 tween，
我们可以使用 `play()`、`pause()`、`reverse()`、`reset()`、`finish()` 和 `seek()` 方法。
本教程演示了每个控制。

{% iframe /downloads/code/tweens/All_Controls.html %}

{% include_code 所有 Tween 控制 tweens/All_Controls.html %}