title: HTML5 Canvas 多重滤镜教程
---

要对 `Konva.Image` 应用多个滤镜，我们必须先使用 `cache()` 函数对其进行缓存。然后使用 `filter()` 函数应用滤镜。

有关所有可用的滤镜，请访问 [滤镜文档](/api/Konva.Filters.html)。

{% iframe /downloads/code/filters/Multiple_Filters.html %}

{% include_code Konva 多重滤镜图像演示 filters/Multiple_Filters.html %}