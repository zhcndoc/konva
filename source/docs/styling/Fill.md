title: HTML5 Canvas 填充教程
---

要使用 Konva 填充形状，我们可以在实例化形状时设置 fill 属性，或者可以使用 `fill()` 方法。

Konva 支持颜色、图案、线性渐变和径向渐变。

说明：鼠标移到每个五边形上可以改变其填充颜色。您还可以拖放这些形状。

{% iframe /downloads/code/styling/Fill.html %}

{% include_code Konva 填充演示 styling/Fill.html %}